# Runs the published image from Docker Hub. To build and run from local
# source instead, see docker-compose-dev.yaml.
services:
  runpod-exporter:
    image: evanofslack/runpod-exporter:latest
    ports:
      - "9836:9836"
    env_file:
      - .env
    restart: unless-stopped
